[task]
goal = "Create a file add.py containing a function add(a, b) that returns a + b. Then run a quick test with Bash (e.g. python3 -c ...) to confirm add(2, 3) == 5, and report the test result."
timeout_sec = 300
