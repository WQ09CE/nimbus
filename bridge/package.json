{
  "name": "nimbus-pi-bridge",
  "version": "0.1.0",
  "description": "Bridge between Nimbus (Python) and Pi (TypeScript)",
  "type": "module",
  "main": "dist/pi-bridge.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/pi-bridge.js",
    "dev": "tsx pi-bridge.ts"
  },
  "dependencies": {
    "@mariozechner/pi-ai": "latest"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "tsx": "^4.0.0"
  }
}
