# 已授权范围与用户端验收

更新：2026-09-11 09:22（北京时间）。Dennis 已通过 Telegram 亲自创建并启用日报，
确认立即试跑和收件大体正常，随后要求改善并发、对话感和去掉调试状态。
本次仅升级这些体验，保留既有任务，不重建、不重复试跑、不修改明早投递时间。

## 已执行的授权

- 专用 PostgreSQL 18.6、受限应用角色、gateway、scheduler 与三个 worker 进程已运行。
  A 专门处理聊天；B/C 提供两个后台执行名额，均在同一台主机上。
- AgentOS 接入代码／文件、X／网页搜索、记忆、任务调度、时钟与实际任务状态查询；
  默认 Astra，搜索 Grok。后台运行时可以继续聊天和追问，不必逐项开启能力。
- 代码／文件只在 rootless Podman＋校验过的 gVisor 中执行；身份、网络、资源和取消边界保持。
- Pi 正常管理 Codex/xAI OAuth 与刷新，没有索取额外 API key、导出 OAuth 或修改全局 Pi 配置。
- `Linger=yes`，服务不依赖开发 Pi 会话。未做注销／冷重启验收，也不保证休眠／关机时准点投递。
- 升级前已备份。既有任务的 ID、启用状态、generation、下次投递时间及指令摘要校验保持不变；
  身份白名单和迁移时的收件游标保持不变。两条旧检索记录来自原始工具日志，不是重搜伪造。

## 统一边界

1. 仍只有 Dennis 已确认的唯一私人 bot/user/chat 身份；没有增加群聊或其他用户权限。
   不抢占别的 poller，不删除 webhook。
2. token／DSN 只在受保护的本机配置中：目录 0700、秘密文件 0600。不能进入仓库、模型、
   命令行参数或截图。worker 不含 bot token；Pi 子进程不继承 DSN/token 环境。
3. 模型不能执行宿主 Bash、读宿主 home／个人浏览器 profile、使用 Docker/DBus/PG socket。
   沙箱无网络、无宿主可写工作区挂载；没有本地 fallback。后台工作区按任务隔离。
4. 没有公开发帖、支付、账号修改等外部写接口。凭据失效须正常重新授权，不能绕过或复制。
5. 日报由 Dennis 本人授权开启，助手不能擅自新增、重跑或改变计划。询问原因／进度不是重跑授权。
   后台不能修改记忆／调度或递归建任务。取消单次执行不会删除每日计划；停止不等于撤回在途发送。

## 接下来验收什么

不需要重建日报。可以直接问：

> 刚才那份日报为什么只有两条？查一下当时的检索记录，直接说明原因，不要重新搜索或重跑。

若本人希望再验并发，可主动要求：

> 把现有日报任务立即运行一次，在后台处理，不要新建定时任务。

然后在后台执行期间发送普通问题或“进度如何”。前台自己有长请求时，新消息仍按顺序处理，
不是无限并行或任意插断。`/status`、`/cancel` 是直接控制通道。仍需观察真实下一次 08:00 投递。

## 运营缺口

尚无定期／加密异地备份、自动内容保留、磁盘告警或完整丢机恢复。Telegram bot 不是端到端加密
聊天，不要发送公司材料和凭据。OAuth 搜索成功不代表搜索用量包干或无费用。
