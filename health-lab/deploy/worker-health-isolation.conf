[Service]
# Direct-path isolation; same-UID hostile processes are not a separate security principal.
# Model tools also lack host filesystem access, and private scopes cannot export tool data.
InaccessiblePaths=%h/.local/share/garmin-readiness-cn %h/.config/garmin-readiness-cn
