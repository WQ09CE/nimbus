# Task: Read and Fix a Buggy Sorting Module

There is a file called `sorter.py` in the current working directory. It contains several sorting-related functions, but some of them have bugs.

Your task:
1. Read the file `sorter.py`
2. Identify and fix ALL bugs
3. Save the corrected version

## Expected behavior

- `bubble_sort(lst)`: Sort a list in ascending order using bubble sort
- `find_min(lst)`: Return the minimum element in a list
- `is_sorted(lst)`: Return True if the list is sorted in ascending order, False otherwise

## Notes
- Do NOT add new functions, only fix existing ones
- The file already exists in the current directory
