# Task: Fix the Bug in calculator.py

There is a file `calculator.py` in the current directory with a simple calculator class. However, it has several bugs. Fix all the bugs so that the tests pass.

## Known Issues

1. The `divide` method doesn't handle division by zero correctly
2. The `multiply` method has a logic error
3. The `subtract` method has the operand order reversed

## Requirements

- Fix all bugs in `calculator.py`
- Do NOT rename the file or change the class/method signatures
- All existing test cases should pass after your fix
